---
title: UDP Client
description: How to use UDP client class
---

import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { ImageZoom } from 'fumadocs-ui/components/image-zoom';

## References
 
<Tabs items={['Include']}>
    <Tab value="Include">#include "UDP/UDPClient.hpp"</Tab>
</Tabs>

## How to use

<Steps>
  <Step>
    Blueprint: search by [`GetInternetProtocolSubsystem`](/docs/Unreal/subsystem#how-to-use) and call [`CreateUDPClient`](/docs/Unreal/subsystem#create-udp-client) function
  </Step>
  <Step>
    C++: Use the code bellow
    ``` cpp
    #include "UDP/UDPClient.h"

    UUDPClient* UdpClient = NewObject<UUDPClient>();
    ```
  </Step>
</Steps>

## Nodes

### Set Host

* Set the `adress` and `port` of host
<ImageZoom src="/img/udp/sethost.png" width={170} height={164} />

### Get Local Adress

* Get local IP of client
<ImageZoom src="/img/udp/getlocaladress.png" width={227} height={76} />

### Get Local Port

* Get local port of client
<ImageZoom src="/img/udp/getlocalport.png" width={227} height={76} />

### Get Remote Adress

* Get host IP
<ImageZoom src="/img/udp/getremotehost.png" width={227} height={76} />

### Get Remote Port

* Get host port
<ImageZoom src="/img/udp/getremotehost.png" width={227} height={76} />

### Set Timeout

* Set timeout in seconds
* OBS: Value must be between 0 and 255
<ImageZoom src="/img/udp/settimeout.png" width={170} height={134} />

### Get Timeout

* Get timeout in seconds
<ImageZoom src="/img/udp/gettimeout.png" width={227} height={76} />

### Set Max Attemp

* Set max times to try reconnect
* OBS: Value must be between 0 and 255
<ImageZoom src="/img/udp/setmaxattemp.png" width={170} height={134} />

### Get Max Attemp

* Get max times to try reconnect
<ImageZoom src="/img/udp/getmaxattemp.png" width={227} height={76} />

### Set Max Send Buffer Size

* Set max size in bytes of send buffer
<ImageZoom src="/img/udp/setmaxsendbuffer.png" width={213} height={134} />

### Get Max Send Buffer Size

* Get max size in bytes of send buffer
<ImageZoom src="/img/udp/getmaxsendbuffer.png" width={227} height={76} />

### Set Max Receive Buffer Size

* Set max size in bytes of receive buffer
<ImageZoom src="/img/udp/setmaxreceivebuffer.png" width={230} height={134} />

### Get Max Receive Buffer Size

* Get max size in bytes of receive buffer
<ImageZoom src="/img/udp/getmaxreceivebuffer.png" width={231} height={76} />

### Set Split Package

* If the size of a message exceeds the maximum allowed size for the send buffer, it will be divided into smaller packets before being sent.
<ImageZoom src="/img/udp/setsplitpackage.png" width={170} height={136} />

### Get Split Package

* Get if Split Package is enabled
<ImageZoom src="/img/udp/getsplitpackage.png" width={227} height={76} />

### Send

* Send a string message
* Return true if It's able to send
* Warning: If you’re using Windows, ensure that you are sending characters compatible with UTF-8
<ImageZoom src="/img/udp/send.png" width={229} height={134} />

### Send Raw

* Send a buffer message
* Return true if It's able to send
<ImageZoom src="/img/udp/sendraw.png" width={227} height={130} />

### Async Read

* In case an error occurs during the asynchronous message reception, you can use the function below to reactivate the async reading
* Return true if It's able to read
<ImageZoom src="/img/udp/asyncread.png" width={226} height={104} />

### Connect

* Connect to the host
* Return true if It's able to connect
<ImageZoom src="/img/udp/connect.png" width={207} height={94} />

### Is Connected

* Check if client is connected to the host
<ImageZoom src="/img/udp/isconnected.png" width={227} height={76} />

### Close

* Close connection
<ImageZoom src="/img/udp/close.png" width={170} height={104} />

### Get Error Code

* Get latest error code
<ImageZoom src="/img/udp/geterrorcode.png" width={227} height={76} />

### Get Error Message

* Get latest error message
<ImageZoom src="/img/udp/geterrormessage.png" width={227} height={76} />

## Events

### On Connected

* Event triggered when a connection is estabilished
<ImageZoom src="/img/udp/onconnected.png" width={225} height={186} />

### On Connection Close

* Event triggered when a connection is closed
<ImageZoom src="/img/udp/onconnectionclose.png" width={263} height={186} />

### On Connection Will Retry

* Event triggered when every time It's try recconnect
<ImageZoom src="/img/udp/onconnectionwillretry.png" width={286} height={212} />

### On Message Sent

* Event triggered when a message is sent
<ImageZoom src="/img/udp/onmessagesent.png" width={246} height={212} />

### On Message Received

* Event triggered when a message is received
<ImageZoom src="/img/udp/onmessagereceived.png" width={272} height={238} />

### On Error

* Event triggered if any error occur during async process
<ImageZoom src="/img/udp/onerror.png" width={192} height={238} />

