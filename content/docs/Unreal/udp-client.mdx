---
title: UDP Client
description: How to use UDP client class
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { ImageZoom } from "fumadocs-ui/components/image-zoom";

## References

<Tabs items={["Include"]}>
    <Tab value="Include">#include "UDP/UDPClient.hpp"</Tab>
</Tabs>

## How to use

<Steps>
  <Step>
    Blueprint: search by [`GetInternetProtocolSubsystem`](/docs/Unreal/subsystem#how-to-use) and call [`CreateUDPClient`](/docs/Unreal/subsystem#create-udp-client) function
  </Step>
  <Step>
    C++: Use the code bellow
    ``` cpp
    #include "UDP/UDPClient.h"

    UUDPClient* UdpClient = NewObject<UUDPClient>();
    ```

  </Step>
</Steps>

## Nodes

### Set Host

-   Set the `adress` and `port` of host
    <ImageZoom
        src="/img/client-side/udp/sethost.png"
        alt="UDP client"
        width={262}
        height={213}
    />

### Get Socket

-   Get socket properties
    <ImageZoom
        src="/img/client-side/udp/getsocket.png"
        alt="UDP client"
        width={130}
        height={56}
    />

### Set Max Send Buffer Size

-   Set max size in bytes of send buffer
    <ImageZoom
        src="/img/client-side/udp/setmaxsendbuffer.png"
        alt="UDP client"
        width={213}
        height={134}
    />

### Get Max Send Buffer Size

-   Get max size in bytes of send buffer
    <ImageZoom
        src="/img/client-side/udp/getmaxsendbuffer.png"
        alt="UDP client"
        width={227}
        height={76}
    />

### Set Max Receive Buffer Size

-   Set max size in bytes of receive buffer
    <ImageZoom
        src="/img/client-side/udp/setmaxreceivebuffer.png"
        alt="UDP client"
        width={230}
        height={134}
    />

### Get Max Receive Buffer Size

-   Get max size in bytes of receive buffer
    <ImageZoom
        src="/img/client-side/udp/getmaxreceivebuffer.png"
        alt="UDP client"
        width={231}
        height={76}
    />

### Set Split Package

-   If the size of a message exceeds the maximum allowed size for the send buffer, it will be divided into smaller packets before being sent.
    <ImageZoom
        src="/img/client-side/udp/setsplitpackage.png"
        alt="UDP client"
        width={170}
        height={136}
    />

### Get Split Package

-   Get if Split Package is enabled
    <ImageZoom
        src="/img/client-side/udp/getsplitpackage.png"
        alt="UDP client"
        width={227}
        height={76}
    />

### Send Str

-   Send a string message
-   Return false if message is empty or socket is closed
-   Warning: If you’re using Windows, ensure that you are sending characters compatible with UTF-8
    <ImageZoom
        src="/img/client-side/udp/sendstr.png"
        alt="UDP client"
        width={229}
        height={134}
    />

### Send Buffer

-   Send a buffer message
-   Return false if buffer is empty or socket is closed
    <ImageZoom
        src="/img/client-side/udp/sendbuffer.png"
        alt="UDP client"
        width={228}
        height={130}
    />

### Connect

-   Connect to the host
-   Return true if It's able to connect
    <ImageZoom
        src="/img/client-side/udp/connect.png"
        alt="UDP client"
        width={207}
        height={94}
    />

### Close

-   Close connection
    <ImageZoom
        src="/img/client-side/udp/close.png"
        alt="UDP client"
        width={170}
        height={104}
    />

### Get Error Code

-   Get latest error code
    <ImageZoom
        src="/img/client-side/udp/geterrorcode.png"
        alt="UDP client"
        width={228}
        height={76}
    />

## Events

### On Connected

-   Event triggered when a connection is estabilished
    <ImageZoom
        src="/img/client-side/udp/onconnectedevent.png"
        alt="UDP client"
        width={225}
        height={186}
    />

### On Bytes Transferred

-   Event triggered when socket send or receive data
    <ImageZoom
        src="/img/client-side/udp/onbytestransferredevent.png"
        alt="UDP client"
        width={269}
        height={238}
    />

### On Message Sent

-   Event triggered when a message is sent
    <ImageZoom
        src="/img/client-side/udp/onmessagesentevent.png"
        alt="UDP client"
        width={246}
        height={212}
    />

### On Message Received

-   Event triggered when a message is received
    <ImageZoom
        src="/img/client-side/udp/onmessagereceivedevent.png"
        alt="UDP client"
        width={272}
        height={238}
    />

### On Close

-   Event triggered when socket is closed
    <ImageZoom
        src="/img/client-side/udp/oncloseevent.png"
        alt="UDP client"
        width={195}
        height={186}
    />

### On Error

-   Event triggered if any error occur during async process
    <ImageZoom
        src="/img/client-side/udp/onerrorevent.png"
        alt="UDP client"
        width={192}
        height={212}
    />
