---
title: Websocket Server Openssl
description: How to use Websocket server class with openssl
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { ImageZoom } from "fumadocs-ui/components/image-zoom";

## References

<Tabs items={["Include"]}>
    <Tab value="Include">#include "websocket/wsserver.h"</Tab>
</Tabs>

## How to use

<Steps>
  <Step>
    Blueprint: search by [`GetInternetProtocolSubsystem`](/docs/Unreal/subsystem#how-to-use) and call [`CreateWebsocketServerSsl`](/docs/Unreal/subsystem#create-websocket-server-ssl) function
  </Step>
  <Step>
    C++: Use the code bellow
    ``` cpp
    #include "websocket/wsserver.h"

    FSecurityContextOpts SecOpts = { ... };
    UWSServerSsl* WSServerSsl = NewObject<UWSServerSsl>();
    client->Construct(SecOpts);
    ```

  </Step>
</Steps>

## Functions

This class has the same functions from `WSServer`.
