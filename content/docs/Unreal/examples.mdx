---
title: Examples / Demo
description: Setup demo project
---

import { Step, Steps } from "fumadocs-ui/components/steps";

# Plugin Demo

If you wish to test the plugin power before buy or use follow the instruction bellow.

<Steps>
    <Step>Download [`Bun`](https://bun.sh/) and install</Step>
    <Step>
        Download
        [`Demo.zip`](https://github.com/Cesio137/InternetProtocol/releases) and
        [`Bun.zip`](https://github.com/Cesio137/InternetProtocol/releases) and
        extract file
    </Step>
    <Step>Open a terminal inside `Bun` and type `bun run ue-server`</Step>
    <Step>
        Run as many instance do you wish and connect to go server using
        `localhost` as adress and `8080` as port
    </Step>
</Steps>

# Plugin Example

If you wish to test the websocket and http examples follow the instruction bellow.

## Setup

<Steps>
  <Step>
    Download [`Bun`](https://bun.sh/) and install
  </Step>
  <Step>
    Download [`Bun.zip`](https://github.com/Cesio137/InternetProtocol/releases) and extract file
  </Step>
  <Step>
    Go to `Bun` folder(extracted from Bun.zip), open terminal and type `bun run dev <resolver>`
  </Step>
  <Step>
    Change `<resolver>` by: `udp-client` `tcp-client`, `http-client`, `ws-client`, `udp-server` `tcp-server`, `http-server`, `ws-server`
  </Step>
  <Step>
    Now choose a unreal map and connect to server
  </Step>
</Steps>
