---
title: HTTP Client
description: How to use HTTP client class
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { ImageZoom } from "fumadocs-ui/components/image-zoom";

## References

<Tabs items={["Include"]}>
    <Tab value="Include">#include "http/httpclient.h"</Tab>
</Tabs>

## How to use

<Steps>
  <Step>
    Blueprint: search by [`GetInternetProtocolSubsystem`](/docs/Unreal/subsystem#how-to-use) and call [`CreateHttpClient`](/docs/Unreal/subsystem#create-http-client) function
  </Step>
  <Step>
    C++: Use the code bellow
    ``` cpp
    #include "http/httpclient.h"

    UHttpClient* client = NewObject<UHttpClient>();
    ```

  </Step>
</Steps>

## Variables

### Idle Timeout Seconds

-    Set/Get timeout value in seconds

<UVar 
  pin={{ name: "Idle Timeout Seconds", pintype: "byte" }}
  get={true}
/>

## Functions

### Is Open

-   Return true if socket is open.

<UFunction
  name="Is Open"
  pure={true}
  outPins={[
    { name: "Return Value", pintype: "bool" }
  ]}
/>

### Set Host

-    Set address and port to resolve. Must be called before use 'Request' function.

<UFunction
  name="Set Host"
  className="Http Client"
  pure={false}
  inPins={[
    { name: "", pintype: "exec" },
    { name: "Target", pintype: "object" },
    { name: "Request", pintype: "struct" },
  ]}
  outPins={[
    { name: "", pintype: "exec" }
  ]}
/>

### Request

-   Process payload and send request.

<UFunction
  name="Request"
  className="Http Client"
  pure={false}
  inPins={[
    { name: "", pintype: "exec" },
    { name: "Target", pintype: "object" },
    { name: "Request", pintype: "struct" },
    { name: "Callback", pintype: "delegate" }
  ]}
  outPins={[
    { name: "", pintype: "exec" }
  ]}
/>

<UEvents
  name="On Request"
  showBindNode={false}
  outPins={[
    { name: "", pintype: "exec" },
    { name: "Error Code", pintype: "struct" },
    { name: "Response", pintype: "struct" },
  ]}
/>

### Close

-   Close the underlying socket and stop listening for data on it. Also can be used to force cancel the request process.

<UFunction
  name="Close"
  className="Http Client"
  pure={false}
  inPins={[
    { name: "", pintype: "exec" },
    { name: "Target", pintype: "object" }
  ]}
  outPins={[
    { name: "", pintype: "exec" }
  ]}
/>