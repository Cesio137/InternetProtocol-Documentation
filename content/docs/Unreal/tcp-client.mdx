---
title: TCP Client
description: How to use TCP client class
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { ImageZoom } from "fumadocs-ui/components/image-zoom";

## References

<Tabs items={["Include"]}>
    <Tab value="Include">#include "TCP/TCPClient.h"</Tab>
</Tabs>

## How to use

<Steps>
  <Step>
    Blueprint: search by [`GetInternetProtocolSubsystem`](/docs/Unreal/subsystem#how-to-use) and call [`CreateTCPClient`](/docs/Unreal/subsystem#create-tcp-client) function
  </Step>
  <Step>
    C++: Use the code bellow
    ``` cpp
    #include "TCP/TCPClient.h"

    UTCPClient* TcpClient = NewObject<UTCPClient>();
    ```

  </Step>
</Steps>

## Functions

### Set Host

-   Set the `adress` and `port` of host
    <ImageZoom
        src="/img/client-side/tcp/sethost.png"
        alt="TCP client"
        width={169}
        height={164}
    />

### Get Socket

-   Get socket properties
    <ImageZoom
        src="/img/client-side/tcp/getsocket.png"
        alt="TCP client"
        width={130}
        height={75}
    />

### Set Max Send Buffer Size

-   Set max size in bytes of send buffer
    <ImageZoom
        src="/img/client-side/tcp/setmaxsendbuffer.png"
        alt="TCP client"
        width={213}
        height={134}
    />

### Get Max Send Buffer Size

-   Get max size in bytes of send buffer
    <ImageZoom
        src="/img/client-side/tcp/getmaxsendbuffer.png"
        alt="TCP client"
        width={228}
        height={76}
    />

### Set Split Package

-   If the size of a message exceeds the maximum allowed size for the send buffer, it will be divided into smaller packets before being sent.
    <ImageZoom
        src="/img/client-side/tcp/setsplitpackage.png"
        alt="TCP client"
        width={169}
        height={136}
    />

### Get Split Package

-   Get if Split Package is enabled
    <ImageZoom
        src="/img/client-side/tcp/getsplitpackage.png"
        alt="TCP client"
        width={228}
        height={76}
    />

### Send Str

-   Send a string message
-   Return true if It's able to send
-   Warning: If you’re using Windows, ensure that you are sending characters compatible with UTF-8
    <ImageZoom
        src="/img/client-side/tcp/sendstr.png"
        alt="TCP client"
        width={229}
        height={134}
    />

### Send Buffer

-   Send a buffer message
-   Return true if It's able to send
    <ImageZoom
        src="/img/client-side/tcp/sendbuffer.png"
        alt="TCP client"
        width={228}
        height={130}
    />

### Connect

-   Connect to the host
-   Return true if It's able to connect
    <ImageZoom
        src="/img/client-side/tcp/connect.png"
        alt="TCP client"
        width={227}
        height={104}
    />

<UFunction
  name="Connect"
  className="TCPClient"
  pure={false}
  inPins={[
    { name: "", pintype: "exec" },
    { name: "Target", pintype: "object" },
    { name: "Bind Opts", pintype: "struct" }
  ]}
  outPins={[
    { name: "", pintype: "exec" },
    { name: "Return Value", pintype: "bool" }
  ]}
/>

### Close

-   Close connection
    <ImageZoom
        src="/img/client-side/tcp/close.png"
        alt="TCP client"
        width={166}
        height={103}
    />

### Get Error Code

-   Get latest error code
    <ImageZoom
        src="/img/client-side/tcp/geterrorcode.png"
        alt="TCP client"
        width={227}
        height={76}
    />

## Events

### On Connected

-   Event triggered when a connection is estabilished
    <ImageZoom
        src="/img/client-side/tcp/onconnectedevent.png"
        alt="TCP client"
        width={225}
        height={186}
    />

### On Bytes Transferred

-   Event triggered when socket send or receive data
    <ImageZoom
        src="/img/client-side/tcp/onbytestransferedevent.png"
        alt="TCP client"
        width={258}
        height={238}
    />

### On Message Sent

-   Event triggered when a message is sent
    <ImageZoom
        src="/img/client-side/tcp/onmessagesentevent.png"
        alt="TCP client"
        width={246}
        height={212}
    />

### On Message Received

-   Event triggered when a message is received
    <ImageZoom
        src="/img/client-side/tcp/onmessagereceivedevent.png"
        alt="TCP client"
        width={272}
        height={212}
    />

### On Close

-   Event triggered when socket is closed
    <ImageZoom
        src="/img/client-side/tcp/oncloseevent.png"
        alt="TCP client"
        width={195}
        height={186}
    />

### On Error

-   Event triggered if any error occur during async process
    <ImageZoom
        src="/img/client-side/tcp/onerrorevent.png"
        alt="TCP client"
        width={192}
        height={212}
    />
