---
title: TCP Client
description: How to use TCP client class
---

import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { ImageZoom } from 'fumadocs-ui/components/image-zoom';

## References
 
<Tabs items={['Include']}>
    <Tab value="Include">#include "TCP/TCPClient.h"</Tab>
</Tabs>

## How to use

<Steps>
  <Step>
    Blueprint: search by [`GetInternetProtocolSubsystem`](/docs/Unreal/subsystem#how-to-use) and call [`CreateTCPClient`](/docs/Unreal/subsystem#create-tcp-client) function
  </Step>
  <Step>
    C++: Use the code bellow
    ``` cpp
    #include "TCP/TCPClient.h"

    UTCPClient* TcpClient = NewObject<UTCPClient>();
    ```
  </Step>
</Steps>

## Functions

### Set Host

* Set the `adress` and `port` of host
<ImageZoom src="/img/tcp/sethost.png" width={169} height={164} />

### Get Local Adress

* Get local IP of client
<ImageZoom src="/img/tcp/getlocaladress.png" width={227} height={76} />

### Get Local Port

* Get local port of client
<ImageZoom src="/img/tcp/getlocalport.png" width={227} height={76} />

### Get Remote Adress

* Get host IP
<ImageZoom src="/img/tcp/getremoteadress.png" width={227} height={76} />

### Get Remote Port

* Get host port
<ImageZoom src="/img/tcp/getremoteport.png" width={227} height={76} />

### Set Timeout

* Set timeout in seconds
* OBS: Value must be between 0 and 255
<ImageZoom src="/img/tcp/settimeout.png" width={169} height={134} />

### Get Timeout

* Get timeout in seconds
<ImageZoom src="/img/tcp/gettimeout.png" width={228} height={76} />

### Set Max Attemp

* Set max times to try reconnect
* OBS: Value must be between 0 and 255
<ImageZoom src="/img/tcp/setmaxattemp.png" width={169} height={134} />

### Get Max Attemp

* Get max times to try reconnect
<ImageZoom src="/img/tcp/getmaxattemp.png" width={228} height={76} />

### Set Max Send Buffer Size

* Set max size in bytes of send buffer
<ImageZoom src="/img/tcp/setmaxsendbuffer.png" width={213} height={134} />

### Get Max Send Buffer Size

* Get max size in bytes of send buffer
<ImageZoom src="/img/tcp/getmaxsendbuffer.png" width={228} height={76} />

### Set Split Package

* If the size of a message exceeds the maximum allowed size for the send buffer, it will be divided into smaller packets before being sent.
<ImageZoom src="/img/tcp/setsplitpackage.png" width={169} height={136} />

### Get Split Package

* Get if Split Package is enabled
<ImageZoom src="/img/tcp/getsplitpackage.png" width={228} height={76} />

### Send

* Send a string message
* Return true if It's able to send
* Warning: If you’re using Windows, ensure that you are sending characters compatible with UTF-8
<ImageZoom src="/img/tcp/send.png" width={229} height={134} />

### Send Raw

* Send a buffer message
* Return true if It's able to send
<ImageZoom src="/img/tcp/sendraw.png" width={228} height={130} />

### Async Read

* In case an error occurs during the asynchronous message reception, you can use the function below to reactivate the async reading
* Return true if It's able to read
<ImageZoom src="/img/tcp/asyncread.png" width={228} height={104} />

### Connect

* Connect to the host
* Return true if It's able to connect
<ImageZoom src="/img/tcp/connect.png" width={227} height={104} />

### Is Connected

* Check if client is connected to the host
<ImageZoom src="/img/tcp/isconnected.png" width={227} height={76} />

### Close

* Close connection
<ImageZoom src="/img/tcp/close.png" width={166} height={103} />

### Get Error Code

* Get latest error code
<ImageZoom src="/img/tcp/geterrorcode.png" width={227} height={76} />

### Get Error Message

* Get latest error message
<ImageZoom src="/img/tcp/geterrormessage.png" width={227} height={76} />

## Events

### On Connected

* Event triggered when a connection is estabilished
<ImageZoom src="/img/tcp/onconnected.png" width={225} height={186} />

### On Connection Close

* Event triggered when a connection is closed
<ImageZoom src="/img/tcp/onconnectionclose.png" width={263} height={186} />

### On Connection Will Retry

* Event triggered when every time It's try recconnect
<ImageZoom src="/img/tcp/onconnectionwillretry.png" width={286} height={212} />

### On Message Sent

* Event triggered when a message is sent
<ImageZoom src="/img/tcp/onmessagesent.png" width={246} height={212} />

### On Message Received

* Event triggered when a message is received
<ImageZoom src="/img/tcp/onmessagereceived.png" width={272} height={238} />

### On Error

* Event triggered if any error occur during async process
<ImageZoom src="/img/tcp/onerror.png" width={192} height={238} />